<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta http-equiv='X-UA-Compatible' content='IE=edge' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <style>

      :root { --primary: #02c5f5; --secondary: #92fe9e; --dark:#374151;
      --darker: #111827; --blue: #3b82f6; --shadow-xl: 0 20px 25px -5px rgb(0 0
      0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1) } body { margin: 0; padding: 0;
      box-sizing: border-box;line-height: 1.5; -webkit-font-smoothing:
      antialiased; background: linear-gradient(45deg,var(--primary),
      var(--secondary)); color: var(--dark,black); display: flex;
      justify-content: center; align-items: center; width: 100%; min-height:
      100vh;font-size: 16px; } img, picture, video, canvas, svg { display:
      block; max-width: 100%; } .reset-card {text-align: center; padding: 20px;
      background-color: white; width: 500px; height: 100%; border-radius:
      5px;margin: 20px 0; box-shadow: var(--shadow-xl); } .reset-card > div {
      padding-bottom: 20px; } .reset-link { margin-top: 15px;} .reset-link a {
      padding: 15px 25px; background: linear-gradient(45deg,var(--primary),
      var(--secondary)); color: var(--dark); border-radius: 5px;
      text-decoration: none; font-weight: 500;} .reset-info {font-size: 15px;}
    </style>
    <title>ClickToBuy | Password Reset Email</title>
  </head>
  <body>
    {{{body}}}
  </body>
</html>